# DSTT 新機能設計: 抜き出しツール

## 1. 目的

「抜き出しツール」は、PDF または画像ファイルに対して色ベースの加工を行う機能です。

提供する中核機能は以下の 3 つです。

1. **指定の色のみ抜き出す**（それ以外は透明化または白化）
2. **指定の色のみ除外する**（指定色のみ透明化または白化）
3. **指定の色を別の色へ変更する**

想定ユースケース:

- 書類の赤ペン記入だけを除去して読みやすくする
- 印影・マーカー色だけ抽出して監査する
- スキャン書類の白背景のみ透明化し PNG 化する

---

## 2. 要件定義

### 2.1 入力

- 対応形式: `PDF`, `PNG`, `JPG/JPEG`, `WEBP`（初期対応）
- 最大ファイルサイズ: 100MB（暫定）
- PDF は複数ページ対応

### 2.2 出力

- 入力画像の場合
  - 出力: `PNG`（透過保持）、または `JPG`（透過不可のため白埋め）
- 入力 PDF の場合
  - 標準出力: ページごとに処理した `PDF`
  - オプション出力: ページ画像の ZIP（デバッグ向け）

### 2.3 操作モード

- `extract`: 指定色のみ残す
- `exclude`: 指定色のみ消す
- `replace`: 指定色を別色へ置換
- （拡張）`background_to_alpha`: 指定色（例: 白）を透明化

### 2.4 色指定

- UI 入力: HEX（`#RRGGBB`）
- 対応: スポイトで画像上の色サンプル取得（EyeDropper API + フォールバック）
- 対応: 複数色指定（OR 条件で一致判定、色リスト管理UI）
- 一致判定:
  - RGB 直接一致（厳密）
  - 許容誤差（Tolerance）ありの近傍一致（推奨）

---

## 3. 色一致アルゴリズム

### 3.1 基本方針

RGB での単純距離でも実装可能ですが、実運用では同じ赤でもスキャン・圧縮で値が揺れるため、
**Lab 色空間 + DeltaE（色差）**を採用します。

### 3.2 判定フロー

1. 入力画像を RGBA に正規化
2. ピクセル色を Lab へ変換
3. 対象色（target）との DeltaE を計算
4. `DeltaE <= tolerance` を「一致」とみなす
5. モードに応じて画素を更新

### 3.3 推奨初期値

- tolerance: 12（UI で 0〜50 調整可能）
- エッジにじみ対策: 一致マスクへ軽微なモルフォロジー（膨張/収縮）を任意適用

---

## 4. 処理パイプライン設計

### 4.1 画像入力時

1. Pillow で読み込み
2. RGBA 化
3. 色マスク生成（DeltaE + tolerance）
4. モードに応じた変換
5. PNG/JPG で保存

### 4.2 PDF 入力時

1. `pypdfium2` または `PyMuPDF` でページを画像レンダリング
2. 各ページ画像に 4.1 の処理を適用
3. 画像を再結合して PDF 化
   - 文字レイヤ維持よりも **初期版は見た目優先の画像 PDF 再生成**
4. 出力ファイル返却

> 注: 将来、PDF 内のベクターオブジェクト色を書き換える高精度モードを追加可能（別タスク）。

---

## 5. DSTT への組み込み構成

### 5.1 ファイル配置案

- `app/tools/color_extract.py`
  - Blueprint: `color_extract_bp`
  - ルート: `/tools/color_extract`
- `app/templates/color_extract.html`
  - ファイルアップロード UI
  - モード選択
  - 色指定・許容誤差スライダー
  - 出力形式指定

### 5.2 サービス分離（推奨）

処理ロジックを Blueprint から分離し、テスト可能性を高めます。

- `app/services/color_ops.py`
  - `build_color_mask(...)`
  - `apply_extract(...)`
  - `apply_exclude(...)`
  - `apply_replace(...)`
- `app/services/pdf_ops.py`
  - `render_pdf_to_images(...)`
  - `images_to_pdf(...)`

### 5.3 既存登録箇所

- `app/__init__.py` に Blueprint 登録
- `app/templates/index.html` にカード追加

---

## 6. UI/UX 仕様（初期版）

- 入力:
  - ファイル選択（PDF/画像）
  - モード選択（extract/exclude/replace）
  - 対象色（HEX）
  - 置換先色（replace 時のみ）
  - tolerance（スライダー）
  - 透明化するか（checkbox）
- 出力:
  - リアルタイムプレビュー（入力やスライダー変更時に自動更新）
  - ダウンロードボタン
- バリデーション:
  - HEX 形式チェック
  - PDF のとき JPG 出力指定を禁止（初期実装では PNG/PDF のみ許可）


### 6.1 リアルタイムプレビュー仕様

- プレビューAPI: `/tools/color_extract/preview`
- 入力イベント（色追加/削除、スポイト、tolerance、モード変更）を 300〜500ms デバウンスして再描画
- PDF は初期版で先頭ページのみプレビューし、本処理では全ページ反映
- 色取得フォールバックとして、プレビュー画像クリックで画素色を採取

---

## 7. エラーハンドリング

- 非対応フォーマット: `400 Bad Request`
- 巨大ファイル: `413 Payload Too Large`
- PDF レンダリング失敗: ユーザー向けメッセージ + サーバーログ詳細
- メモリ不足対策:
  - ページ単位逐次処理
  - DPI 制限（例: 200dpi 上限）

---

## 8. パフォーマンス方針

- NumPy ベクトル演算で画素処理（for ループ回避）
- 大サイズ画像は自動縮小オプション
- PDF はページ並列（将来）
- キャッシュは初期版では非採用（再現性優先）

---

## 9. セキュリティ/運用

- アップロードファイル名は `secure_filename` を利用
- 一時ファイルはジョブ完了後に削除
- MIME と拡張子の二重検証
- ログに原本ファイル名や個人情報を残しすぎない

---

## 10. 実装ステップ（提案）

### Phase 1: 画像のみ MVP

- `extract/exclude/replace` を PNG/JPG 入力で実装
- tolerance + HEX 指定 UI
- 単体テスト: マスク判定とモード処理

### Phase 2: PDF 対応

- PDF→画像→PDF の一連処理
- 複数ページ一括処理
- エラーハンドリング強化

### Phase 3: 品質改善

- スポイト UI
- 背景透明化プリセット（白抜き）
- 色判定の高速化/改善

---

## 11. 受け入れ基準（Acceptance Criteria）

1. 赤色除外モードで、赤ペン追記が視認上ほぼ除去できる
2. replace モードで、指定色が別色へ一貫して置換される
3. 白背景透明化で PNG 出力時にアルファが保持される
4. PDF 複数ページ処理でページ順・ページ数が維持される
5. 失敗時にユーザーが原因を理解できるエラー文言が表示される

---

## 12. 推奨ライブラリ

- 画像: `Pillow`, `numpy`
- 色差: `colormath` または `opencv-python`（Lab変換）
- PDF: `pypdfium2`（レンダリング品質/速度のバランス）

`requirements.txt` には段階的に追加し、まずは MVP に必要最小限を導入します。
