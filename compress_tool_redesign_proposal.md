# 圧縮ファイル生成ツール 高機能化＆UIリデザイン提案

## 1. 目的

既存の「圧縮ファイル生成ツール」を、
- **一括リネームツールのUIトーンを踏襲**し、
- **業務利用で不足しがちな機能を補強**し、
- **現在の実装上の不具合/設計上の弱点を解消**
する形で刷新する。

---

## 2. 現状整理（短評）

- 現在の圧縮ツールは、単純なフォーム中心で機能は最小構成。
- 一方で一括リネームは「カード型レイアウト」「ドロップゾーン」「チップ表示」「モード選択」など、操作の分かりやすさと統一感が高い。
- 今回は**操作体験をリネーム側に寄せる**ことで、同一製品内のUI整合性を確保する。

---

## 3. UIリデザイン提案（リネームツール踏襲）

### 3-1. 画面構成

1. **ヘッダー（グラデーション）**
   - タイトル: 「圧縮ファイル生成ツール」
   - サブタイトル: 「複数形式・暗号化・除外設定に対応」

2. **ファイル投入エリア（ドロップゾーン）**
   - ドラッグ&ドロップ + クリック選択
   - 選択ファイルをチップで表示（拡張子バッジ、個別削除）
   - 合計件数/総容量を即時表示

3. **圧縮モード選択（モードカード）**
   - ZIP
   - TAR
   - TAR.GZ
   - （将来的に）7Z ※サーバー依存のため段階導入

4. **詳細オプションパネル**
   - 圧縮レベル
   - アーカイブ名
   - ルートフォルダ名（包み込み）
   - パスワード（ZIP時のみ表示）
   - 分割サイズ（例: 100MBごと）
   - 除外条件（拡張子、ファイル名パターン）

5. **プレビュー／検証エリア**
   - 生成予定ファイル名（例: `案件A_20260225.zip`）
   - 予想件数（除外後）
   - 注意喚起（同名衝突/無効文字/空選択）

6. **実行ボタン群**
   - 「圧縮してダウンロード」
   - 「設定をリセット」

### 3-2. デザインルール

- リネームツールの配色変数とカード/ボタン/チップのコンポーネント構造を再利用。
- 余白、角丸、影、入力フォーカスのトーンを統一。
- 「派手さより業務UI」を優先（過度なアニメーションは不要）。

---

## 4. 追加機能提案（優先度つき）

### P1（先行実装推奨）

1. **圧縮前プレビューAPI**
   - 実際に圧縮せず、対象件数・除外結果・出力名を返す。

2. **同名ファイル自動リネーム**
   - `report.pdf`, `report (1).pdf` のように衝突回避。

3. **フォーマットごとの入力制御**
   - ZIP以外ではパスワード入力を無効化し、誤入力を防止。

4. **アーカイブ名のサニタイズ**
   - 禁止文字除去、空文字フォールバック。

5. **入力バリデーション強化**
   - 許可フォーマット以外拒否
   - ファイル数上限/容量上限

### P2（次フェーズ）

6. **除外ルール**
   - 拡張子除外（`.tmp`, `.log` など）
   - パターン除外（ワイルドカード）

7. **フォルダ構造保持オプション**
   - 将来のディレクトリアップロード対応を見据えた設計。

8. **圧縮レベル選択**
   - 速度優先 / 標準 / 高圧縮

9. **ジョブ履歴（直近N件）**
   - 形式、件数、作成日時、再利用可能な設定。

### P3（要件次第）

10. **分割アーカイブ**
    - サイズ指定で分割（大容量配布向け）。

11. **非同期ジョブ化**
    - 大量ファイル時のUIフリーズ回避、進捗表示。

---

## 5. バグ修正・改善ポイント（現行コード起点）

1. **未使用importの整理**
   - `secure_filename` が未使用。

2. **`format` 値のサーバー側検証不足**
   - 想定外値が来た場合でも処理分岐が不明瞭なまま `send_file` に進む可能性。

3. **`archive_name` 未サニタイズ**
   - ファイル名として危険な文字や不正な相対パス断片の混入余地。

4. **同名ファイル上書き問題**
   - 保存時に同名があると後勝ちで上書きされ、欠損につながる。

5. **エラーハンドリング不足**
   - 圧縮失敗時にユーザー向けメッセージが弱く、原因追跡が困難。

6. **UI上の形式連動不足**
   - ZIP以外選択時もパスワード欄が常時表示され、誤操作を誘発。

---

## 6. 実装方針（段階導入）

### Phase 1（安全性＋UI統一）
- UIをリネームデザインへ統一
- `format` / `archive_name` / ファイル重複のバリデーション
- 明示的エラー表示

### Phase 2（使い勝手）
- 除外ルール・圧縮レベル・プレビューAPI
- 設定保持（localStorage）

### Phase 3（大規模対応）
- 非同期処理・進捗・分割アーカイブ

---

## 7. AI臭さを抑える実装上の注意

- 文言を**現場オペレーション語彙**に統一（"実行", "対象", "除外" など）。
- 過剰な説明文は避け、補助文は短く要点のみ。
- アイコン多用を控え、情報密度と可読性を優先。
- ボタン文言は命令形を避け、業務フローに沿った自然な日本語にする。

---

## 8. 次アクション提案

- 本提案のうち **Phase 1** を先に実装。
- 実装後に画面レビュー（PC/モバイル）を実施し、配色や用語を微調整。
- その後、Phase 2のプレビューAPIと除外機能を追加。
